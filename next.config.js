/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/vanessa-psicologa-landing',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

module.exports = nextConfig; 